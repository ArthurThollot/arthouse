[project]
name = "arthouse"
version = "0.1.0"
description = "Daily movie showtimes near you in Amsterdam, aggregated from Pathé, Cineville and Vue"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32",
    "pyyaml>=6.0",
    "jinja2>=3.1",
    "playwright>=1.45",
]
